FROM ubuntu:focal

ARG DEBIAN_FRONTEND=noninteractive
RUN apt -yqq update --no-install-recommends >/dev/null && apt install -yqq --no-install-recommends ca-certificates curl build-essential debhelper
