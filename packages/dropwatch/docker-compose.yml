version: '2'
services:
  nosu-pkg-dropwatch:
    build: ./
    image: nosu-pkg-dropwatch
    privileged: true
    volumes:
      - ".:/mnt"
    working_dir: /mnt
    command: [ "/mnt/build.sh", "/mnt/${ENVFILE}" ]
    network_mode: "host"
